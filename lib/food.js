module.exports = Food;

function Food(x, y, width, height) {
  this.x = x;
  this.y = y;
  this.width = width;
  this.height = height;
}

// var x = Math.floor(Math.random() * (100 - 1 + 1)) + 1;
// var y = Math.random() * 100;
